%h2#cart_title Your Cart
%table#cart
  = render(cart.line_items)
  %tr#total_line
    %td{:colspan => "3"}Total
    %td#total_cell= number_to_currency(cart.total_price)
= button_to 'Empty cart', cart, method: :delete, confirm: 'Are you sure you want to delete the entire cart?'
