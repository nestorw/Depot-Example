<% if notice %>
  <p id="notice"><%= notice %></p>
<% end %>
<h2> Your Cart </h2>
<ul>
  <% @cart.line_items.each do |item| %>
    <li><%= item.quantity %> &times; <%= item.product.title %> </li>
  <% end %>
</ul>

<%= button_to 'Empty cart', @cart, method: :delete,
    confirm: 'Are you sure you want to delete the entire cart?' %>
